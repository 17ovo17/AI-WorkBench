# AI WorkBench 测试执行记录 - 2026-04-25 03:58

## 本轮信息

- 执行日期：2026-04-25 03:58:00
- 执行人：Codex
- 环境：Windows + WSL Ubuntu-24.04 + 本机 MySQL/Redis/Prometheus
- 前端地址：`http://localhost:3000`
- 后端地址：`http://localhost:8080`
- Prometheus 地址：`http://localhost:9090`
- 证据目录：`.test-evidence/`

## 已验证通过

| 模块 | 测试点 | 结果 | 证据 |
| --- | --- | --- | --- |
| 基础健康 | MySQL/Redis/数据源/AI 配置健康 | 通过 | `.test-evidence/20260425-033104/` |
| Prometheus/Categraf | 6 台业务主机基础与业务指标 | 通过 | `.test-evidence/20260425-033104/prometheus-categraf-summary.json` |
| AI 问答 | `198.18.20.11` IP 压力问诊优先 Prometheus | 通过 | `.test-evidence/workbench-ip-pressure.png` |
| 智能诊断 | 手动输入 `198.18.20.20` 发起诊断 | 修复后通过 | `.test-evidence/diagnose-input-regression.png` |
| 智能诊断 | 删除诊断记录二次确认 | 通过 | `.test-evidence/diagnose-delete-confirm.png` |
| API 安全 | XSS/SQL/命令注入/SSRF/路径遍历/Unicode/超长字段 | 修复后通过 | `.test-evidence/api-security-regression-5/api-security-summary.json` |
| 业务拓扑 | 用户新增业务后仅按 6 台 IP 自动发现 | 通过 | `.test-evidence/topology-business-discover.png` |
| 告警中心 | Alertmanager Webhook、XSS 安全展示、标记恢复 | 通过 | `.test-evidence/alerts-webhook-xss.png`, `.test-evidence/alerts-resolve.png` |
| AI 配置 | 已有模型加载、API Key 脱敏 | 通过 | `.test-evidence/settings_ai-verify.png` |
| 数据源 | Prometheus/MySQL 配置加载 | 通过 | `.test-evidence/settings_datasource-verify.png` |
| 探针管理 | 页面加载、无明文 API Key 暴露 | 通过 | `.test-evidence/catpaw-page.png` |

## 已修复缺陷

| ID | 优先级 | 模块 | 问题 | 修复 |
| --- | --- | --- | --- | --- |
| FT-001 | P1 | 智能诊断 UI | `el-select allow-create` 手动输入 IP 后未写入 `targetIP`，点击发起诊断无效 | 改为独立 `el-input` + 下拉选择，支持 Enter 发起 |
| FT-002 | P1 | 诊断 API | `/api/v1/diagnose` 接受 XSS/命令注入/路径遍历字符串作为目标入库 | 后端新增 `cleanIP`，严格校验 IP 并拒绝 link-local/multicast/unspecified |
| FT-003 | P2 | 测试脚本 | PowerShell 旧版本对 `Invoke-WebRequest` 部分响应误报空引用 | 改为加载 `System.Net.Http` 并用 `HttpClient` 记录状态码 |
| FT-004 | P2 | Categraf 假数据 | 缺少 `netstat_tcp_established` 与 `netstat_tcp_time_wait` 标准名 | 生成脚本补齐并刷新 Prometheus block |

## 未完全闭环/阻塞项

| 模块 | 状态 | 原因 | 下一步 |
| --- | --- | --- | --- |
| Windows 远程探针真实安装/卸载/重装 | 未完成 | 本轮未执行远程主机真实改动 | 下一轮使用平台凭证管理对 Windows 主机执行真实生命周期测试 |
| Catpaw Linux 即时巡检完整成功 | 部分完成 | 已改配置为平台 API，但命令执行输出仍较长，尚未确认最终成功上报 agent | 下一轮继续以 `catpaw inspect system local` 验证 report/heartbeat 入库 |
| 诊断历史清理 | 待处理 | 安全测试产生了旧污染记录 | 后续通过 UI 二次确认或管理脚本清理测试记录 |

## 结论

- 当前无新增 P0。
- 已修复 2 个 P1：诊断 UI 手输无效、诊断 API 目标未校验。
- API 安全回归已通过，Prometheus/Categraf 6 主机指标通过，MCP 页面主链路已覆盖核心页面。
- 仍需继续执行 Windows 探针真实生命周期和 Catpaw Linux 巡检上报闭环。

## 2026-04-25 11:23 Catpaw Windows 巡检摘要降噪回归
- 变更点：Windows Catpaw 摘要从极简 CPU/内存/磁盘/网络几行，扩展为结构化关键巡检报告。
- 覆盖插件：windows.cpu、windows.mem、windows.disk、windows.diskio、windows.net、windows.tcpstate、windows.process、windows.service、windows.eventlog、windows.firewall、windows.update。
- 验证方式：通过平台 `/api/v1/remote/exec` 使用已保存 WMI 凭证触发 `192.168.1.7` 的 `C:\catpaw\catpaw.exe inspect --configs C:\catpaw\conf.d`，由 Catpaw 上报新诊断记录。
- 新记录：`1777087333974127793`，时间 `2026-04-25T11:22:14+08:00`。
- API 证据：摘要包含 `Top CPU 进程`、`网卡收发与错误`、`远程管理规则`；`raw_report` 长度约 207607 字符，证明完整原始插件数据仍保留。
- MCP 证据：浏览器打开 `/diagnose`，点击最新 `192.168.1.7` 记录，报告展示基础资源、Top 进程、磁盘/IO、网络/TCP、服务/事件/防火墙、系统补丁；折叠入口显示 `完整原始巡检数据（默认折叠，可展开/下载）`。
- 结论：修复通过。摘要不再过度降噪，原始信息默认折叠但可展开查看。
