# AI WorkBench 任务看板

## 按角色分组

### 一、总则

| 字段 | 说明 |
|------|------|
| 状态 | TODO / IN_PROGRESS / TESTING / DONE / BLOCKED |
| 优先级 | P0 阻塞 / P1 核心 / P2 增强 / P3 优化 |
| 依赖 | 前置任务编号，无依赖写 - |
| 验收 | 具体的通过标准 |
| 工时 | 预估小时数 |

---

## go-backend（Go 后端开发）

### GB-001 数据库 DDL — 新增 4 张表
- 优先级：P0
- 状态：TODO
- 依赖：-
- 描述：在 store.go 的 migrate() 中新增 diagnosis_cases / diagnosis_feedback / ai_settings / runbooks 四张表的 CREATE TABLE
- 验收：`go build` 通过；启动后 `SHOW TABLES` 可见 4 张新表；字段与 docs/Dify集成设计方案.md 一致
- 工时：1h

### GB-002 知识库 CRUD — 案例管理
- 优先级：P0
- 状态：TODO
- 依赖：GB-001
- 描述：新建 internal/knowledge/cases.go，实现 diagnosis_cases 的增删改查 + 分页 + 关键词搜索 + 按根因分类筛选
- 路由：GET/POST/PUT/DELETE /api/v1/knowledge/cases, GET /api/v1/knowledge/cases/:id
- 验收：curl 调用各端点返回正确数据；分页参数 page/limit 生效；FULLTEXT 搜索命中关键词
- 工时：2h

### GB-003 知识库 CRUD — Runbook 管理
- 优先级：P1
- 状态：TODO
- 依赖：GB-001
- 描述：新建 internal/knowledge/runbooks.go，实现 runbooks 的增删改查 + 按分类筛选
- 路由：GET/POST/PUT/DELETE /api/v1/knowledge/runbooks
- 验收：curl 调用各端点正常
- 工时：1.5h

### GB-004 知识库 — 批量导入导出
- 优先级：P1
- 状态：TODO
- 依赖：GB-002
- 描述：POST /api/v1/knowledge/cases/import（接收 JSON 数组）；GET /api/v1/knowledge/cases/export（返回全量 JSON）
- 验收：导出后再导入，数据一致
- 工时：1h

### GB-005 Dify 代理层 — 基础框架
- 优先级：P0
- 状态：TODO
- 依赖：-
- 描述：新建 internal/dify/proxy.go，封装 Dify API 调用（HTTP client + 配置读取 + 健康检查 + 降级处理）
- 配置项：dify.base_url / dify.app_api_key / dify.dataset_api_key（从 config.yaml 读取）
- 验收：HealthCheck() 在 Dify 未启动时返回 false 且不 panic；在 Dify 启动时返回 true
- 工时：2h

### GB-006 Dify 代理层 — 知识库同步
- 优先级：P0
- 状态：TODO
- 依赖：GB-002, GB-005
- 描述：在 proxy.go 中实现 SyncCase(case) — 将案例文本化后调用 Dify 的 create-by-text API 上传到知识库
- 路由：POST /api/v1/knowledge/cases/sync（同步指定案例到 Dify）
- 验收：同步后在 Dify 知识库中可检索到该案例；dify_document_id 回写到 diagnosis_cases 表
- 工时：2h

### GB-007 Dify 代理层 — 工作流执行
- 优先级：P0
- 状态：TODO
- 依赖：GB-005
- 描述：在 proxy.go 中实现 RunWorkflow(inputs) — 调用 Dify 的 /workflows/run API，支持 blocking 和 streaming 两种模式
- 路由：POST /api/v1/diagnosis/start
- 验收：blocking 模式返回完整诊断结果 JSON；streaming 模式返回 SSE 流
- 工时：3h

### GB-008 Dify 代理层 — 反馈标注
- 优先级：P1
- 状态：TODO
- 依赖：GB-005
- 描述：在 proxy.go 中实现 SubmitFeedback(messageID, rating) — 调用 Dify 的 feedbacks API
- 路由：POST /api/v1/diagnosis/feedback
- 验收：反馈存入 diagnosis_feedback 表；同时回写到 Dify 标注
- 工时：1.5h

### GB-009 诊断归档
- 优先级：P1
- 状态：TODO
- 依赖：GB-002, GB-006
- 描述：POST /api/v1/diagnosis/archive — 将诊断结果提取指标特征/根因/处置，存入 diagnosis_cases，并同步到 Dify 知识库
- 验收：归档后案例列表可见新案例；Dify 知识库可检索
- 工时：2h

### GB-010 指标适配层 — 扫描与存储
- 优先级：P1
- 状态：TODO
- 依赖：GB-001
- 描述：新建 internal/metrics/normalizer/，实现：
  - `scanner.go`：调用 Prometheus `/api/v1/label/__name__/values` 扫描全量指标名，存入 metrics_mappings 表
  - `normalizer.go`：从 metrics_mappings 表读取映射，实现 Normalize(rawName) → standardName
  - DDL：新增 metrics_mappings 表（见 Dify 集成设计方案第十章）
- 路由：POST /api/v1/metrics/scan
- 验收：扫描后 metrics_mappings 表有数据；Normalize 正确转换已映射指标
- 工时：2.5h

### GB-011 指标适配层 — AI 自动适配
- 优先级：P1
- 状态：TODO
- 依赖：GB-010
- 描述：将扫描到的未适配指标分批发给 LLM，AI 识别 exporter/标准名/描述/PromQL，结果写回 metrics_mappings 表（status=auto）
- 路由：POST /api/v1/metrics/auto-adapt
- 验收：AI 返回的映射结果正确率 > 80%；status 标记为 auto
- 工时：3h

### GB-011b 指标适配层 — 用户编辑与确认
- 优先级：P1
- 状态：TODO
- 依赖：GB-011
- 描述：用户可编辑单条映射、批量确认 AI 结果、同步到 Dify 知识库
- 路由：GET /api/v1/metrics/mappings, PUT /api/v1/metrics/mappings/:id, POST /api/v1/metrics/mappings/confirm, POST /api/v1/metrics/mappings/sync
- 验收：编辑后 status 变为 custom；确认后变为 confirmed；同步到 Dify 可检索
- 工时：2h

### GB-012 AI 设置 CRUD
- 优先级：P1
- 状态：TODO
- 依赖：GB-001
- 描述：实现 ai_settings 表的读写
- 路由：GET/PUT /api/v1/settings/ai
- 验收：保存后重启 API，配置仍在
- 工时：1h

### GB-013 自定义工具 — OpenAPI 定义
- 优先级：P1
- 状态：TODO
- 依赖：-
- 描述：编写 get_prometheus_metrics 和 get_inspection_report 两个工具的 OpenAPI 3.0 YAML，放在 api/tools/ 目录
- 验收：YAML 格式合法；可通过 Dify 的工具注册 API 导入
- 工时：1h

### GB-014 告警驱动自动诊断
- 优先级：P2
- 状态：TODO
- 依赖：GB-007
- 描述：告警 webhook 接收后，自动调用 Dify 工作流发起诊断
- 验收：发送告警 webhook → 自动生成诊断记录
- 工时：1.5h

### GB-015 诊断报告版本对比
- 优先级：P3
- 状态：TODO
- 依赖：GB-007
- 描述：同一主机多次诊断时，返回与上次诊断的 diff（指标变化、根因是否相同）
- 验收：API 返回 previous_diagnosis 字段和 diff 摘要
- 工时：2h

---

## vue-frontend（Vue 前端开发）

### VF-001 知识库管理页 — 案例列表
- 优先级：P0
- 状态：TODO
- 依赖：GB-002
- 描述：新建 web/src/views/KnowledgeBase.vue，分页列表展示所有案例，支持关键词搜索、按根因分类筛选
- 验收：MCP 浏览器可访问页面；搜索和筛选生效；分页正常
- 工时：3h

### VF-002 知识库管理页 — 案例详情
- 优先级：P0
- 状态：TODO
- 依赖：VF-001
- 描述：点击案例弹出详情 Dialog，显示指标快照（JSON 格式化）、根因描述、处置步骤、评价历史
- 验收：Dialog 正常打开关闭；JSON 格式化可读
- 工时：2h

### VF-003 知识库管理页 — 同步到 Dify
- 优先级：P1
- 状态：TODO
- 依赖：VF-001, GB-006
- 描述：案例列表中每行有"同步到 Dify"按钮，点击后调用后端同步 API，显示成功/失败提示
- 验收：点击后 loading → 成功提示；已同步的案例显示 Dify 文档 ID
- 工时：1h

### VF-004 知识库管理页 — 批量导入导出
- 优先级：P2
- 状态：TODO
- 依赖：VF-001, GB-004
- 描述：页面顶部"导入"按钮（上传 JSON 文件）和"导出"按钮（下载全量 JSON）
- 验收：导出 → 导入后数据一致
- 工时：1.5h

### VF-005 知识库管理页 — Runbook Tab
- 优先级：P2
- 状态：TODO
- 依赖：VF-001, GB-003
- 描述：知识库页面加 el-tabs，第二个 tab 展示 Runbook 列表和 CRUD
- 验收：Runbook 增删改查正常
- 工时：2h

### VF-005b 指标映射管理页
- 优先级：P1
- 状态：TODO
- 依赖：GB-010, GB-011
- 描述：新建 web/src/views/MetricsMapping.vue，功能：
  - 表格展示所有指标映射（原始名/标准名/来源/描述/状态标签）
  - 状态筛选（auto/confirmed/custom/unmapped）
  - 点击行可编辑标准名、描述、PromQL
  - "扫描指标"按钮 → 调用 /metrics/scan
  - "AI 自动适配"按钮 → 调用 /metrics/auto-adapt，loading 状态
  - "批量确认"按钮 → 确认所有 auto 状态的映射
  - "同步到知识库"按钮 → 同步到 Dify
- 验收：MCP 浏览器可操作全部按钮；编辑后保存生效；状态标签颜色区分
- 工时：3h

### VF-006 诊断交互页 — 发起诊断
- 优先级：P0
- 状态：TODO
- 依赖：GB-007
- 描述：新建 web/src/views/DiagnosisWorkflow.vue，输入主机名/业务名 + 发起按钮，调用 /diagnosis/start
- 验收：输入 IP 点击发起 → 请求发出 → 返回结果
- 工时：2h

### VF-007 诊断交互页 — 流式工作流展示
- 优先级：P0
- 状态：TODO
- 依赖：VF-006
- 描述：streaming 模式下，用卡片逐步展示工作流节点状态（检索案例✓ → 查询指标✓ → AI 分析中...）
- 验收：节点状态实时更新；完成后展示最终报告
- 工时：3h

### VF-008 诊断交互页 — 诊断报告卡片
- 优先级：P0
- 状态：TODO
- 依赖：VF-007
- 描述：结构化展示诊断结果（根因、证据列表、处置建议、置信度标签 HIGH/MEDIUM/LOW）
- 验收：报告卡片渲染正确；置信度用颜色区分（绿/黄/红）
- 工时：2h

### VF-009 诊断交互页 — 反馈和归档
- 优先级：P1
- 状态：TODO
- 依赖：VF-008, GB-008, GB-009
- 描述：报告底部三个反馈按钮（准确/部分准确/不准确）+ 归档按钮
- 验收：点击反馈 → 存入数据库 → 按钮状态更新；点击归档 → 案例列表可见
- 工时：2h

### VF-010 AI 基础设置页
- 优先级：P1
- 状态：TODO
- 依赖：GB-012
- 描述：新建 web/src/views/AISettings.vue，表单配置 Dify 地址/API Key/模型名等，保存按钮
- 验收：保存后刷新页面，配置仍在
- 工时：2h

### VF-011 路由注册
- 优先级：P0
- 状态：TODO
- 依赖：VF-001, VF-006, VF-010
- 描述：在 router/index.js 中注册新页面路由，侧边栏导航加入入口
- 验收：侧边栏可见新菜单项；点击跳转正确
- 工时：0.5h

---

## ops-diagnostician（运维诊断专家）

### OD-001 指标映射 — AI 适配 Prompt 设计
- 优先级：P1
- 状态：TODO
- 依赖：-
- 描述：设计 AI 自动适配指标的 prompt，要求 LLM 对每个原始指标名输出：exporter 来源、标准名、中文描述、PromQL 转换公式、正常范围。覆盖 categraf / node_exporter / mysqld_exporter / redis_exporter / oracledb_exporter 五种 exporter
- 验收：prompt 在测试中对常见指标的适配准确率 > 80%
- 工时：2h

### OD-001b 指标映射 — 静态兜底配置
- 优先级：P1
- 状态：TODO
- 依赖：-
- 描述：编写 metrics_mapping.yaml 兜底配置，至少 30 条映射规则，覆盖 CPU/内存/磁盘/网络/MySQL/Redis 六大类
- 验收：YAML 格式合法；覆盖六大类
- 工时：1.5h

### OD-002 诊断工作流 DSL
- 优先级：P0
- 状态：TODO
- 依赖：-
- 描述：编写 Dify 工作流 YAML，节点包括：start → knowledge_retrieval → http_metrics → http_inspection → llm_diagnosis → condition(置信度) → end
- 验收：YAML 格式合法；可导入 Dify；每个节点有中文注释
- 工时：3h

### OD-003 诊断 Prompt 设计
- 优先级：P0
- 状态：TODO
- 依赖：-
- 描述：编写 LLM 诊断节点的 system prompt，要求输出结构化 JSON（root_cause / evidence / treatment / confidence）
- 验收：LLM 输出可被 JSON.parse；包含置信度字段
- 工时：2h

### OD-004 种子案例数据
- 优先级：P1
- 状态：TODO
- 依赖：-
- 描述：编写 10-20 条典型运维诊断案例（CPU 高/内存泄漏/磁盘满/慢查询/连接池满/OOM 等），作为知识库冷启动数据
- 验收：JSON 格式；每条包含 metric_snapshot / root_cause / treatment；可通过 import API 导入
- 工时：2h

### OD-005 Runbook 种子数据
- 优先级：P2
- 状态：TODO
- 依赖：-
- 描述：编写 5-10 条常见故障的标准操作手册（Oracle 表空间满/Redis 内存满/Nginx 502/JVM Full GC 等）
- 验收：Markdown 格式步骤清晰；可通过 API 导入
- 工时：1.5h

---

## qa-tester（质量测试）

### QA-001 知识库 API 全维度测试
- 优先级：P0
- 状态：TODO
- 依赖：GB-002, GB-003
- 描述：基于统一测试基准第八章（T08-xxx）+ 新增知识库专项用例，覆盖四个维度：
  - 功能正确性：案例/Runbook CRUD 正常路径 + 异常输入（空值/超长/特殊字符/重复创建）
  - 安全性：无 token 访问 / SQL 注入尝试 / XSS 输入
  - 用户体验：错误提示是否中文友好 / 分页参数越界时的表现
  - 降级：MySQL 不可用时的 fallback 行为
- 验收：测试报告覆盖 ≥20 个检查项，PASS 率 100%
- 工时：2h

### QA-002 Dify 集成端到端测试
- 优先级：P0
- 状态：TODO
- 依赖：GB-007, OD-002
- 描述：基于统一测试基准第四章（T04-xxx）+ 新增 Dify 工作流专项，覆盖完整链路：
  - 正常路径：发起诊断 → 工作流执行 → 知识库检索 → 指标查询 → 报告生成 → 反馈 → 归档
  - 异常路径：Dify 不可用时降级 / 知识库为空时的表现 / Prometheus 超时
  - 置信度验证：HIGH/MEDIUM/LOW 三种情况的输出差异
  - 归档闭环：归档后案例列表可见 + Dify 知识库可检索
- 验收：测试报告覆盖完整链路，含降级场景
- 工时：3h

### QA-003 前端页面 MCP 全量测试
- 优先级：P0
- 状态：TODO
- 依赖：VF-001, VF-006, VF-010
- 描述：基于统一测试基准第十二章（UI-xxx）+ 新增页面专项，用 MCP 浏览器验证：
  - 知识库页：列表加载 / 搜索 / 筛选 / 详情弹窗 / 同步按钮 / 导入导出
  - 诊断页：输入 / 发起 / 流式展示 / 报告卡片 / 反馈按钮 / 归档按钮
  - AI 设置页：表单填写 / 保存 / 刷新后持久化
  - 回归验证：原有页面（Dashboard/Workbench/Diagnose/Topology/Alerts）未被破坏
- 验收：每个页面核心操作全 PASS，含截图证据
- 工时：3h

### QA-004 降级与恢复测试
- 优先级：P1
- 状态：TODO
- 依赖：GB-005
- 描述：基于统一测试基准第十六章（DR-xxx），验证 Dify 相关降级：
  - Dify API 停止 → 诊断降级到纯 LLM 模式 → 前端显示降级提示
  - Dify API 恢复 → 自动切回 Dify 工作流模式
  - 知识库同步失败 → 不影响案例本地存储
  - 向量库不可用 → 工作流跳过知识库检索步骤
- 验收：所有降级场景有明确提示，不白屏不报错
- 工时：2h

### QA-005 指标适配与安全测试
- 优先级：P1
- 状态：TODO
- 依赖：GB-010, OD-001
- 描述：
  - 指标适配：验证 categraf/node_exporter/mysqld_exporter/redis_exporter 各 5 条映射正确性
  - 安全测试：基于统一测试基准第十一章（SEC-xxx），验证新增 API 的安全性（注入/越权/脱敏）
- 验收：映射全部正确；安全用例全 PASS
- 工时：2h

---

## doc-writer（文档维护）

### DW-001 更新运维手册
- 优先级：P1
- 状态：TODO
- 依赖：所有 GB/VF 任务完成后
- 描述：更新 docs/运维手册.md，补充 Dify 部署/配置/知识库管理/诊断工作流说明
- 验收：新用户按手册可完成 Dify 集成配置
- 工时：2h

### DW-002 更新 README
- 优先级：P1
- 状态：TODO
- 依赖：DW-001
- 描述：更新 README.md，补充知识库和诊断工作流的说明
- 验收：README 反映最新功能
- 工时：0.5h

### DW-003 Dify 部署文档
- 优先级：P0
- 状态：TODO
- 依赖：GB-005
- 描述：编写 Dify sidecar 部署说明（Python 依赖/环境变量/数据库初始化/启动命令）
- 验收：新环境按文档可启动 Dify
- 工时：1.5h

### DW-004 API 文档
- 优先级：P2
- 状态：TODO
- 依赖：所有 GB 任务完成后
- 描述：更新所有新增 API 的文档（路径/参数/响应示例）
- 验收：每个新 API 有完整的请求/响应示例
- 工时：2h

---

## 任务统计

| 角色 | 任务数 | P0 | P1 | P2 | P3 | 总工时 |
|------|--------|----|----|----|----|--------|
| go-backend | 15 | 5 | 6 | 2 | 2 | 24h |
| vue-frontend | 11 | 5 | 3 | 2 | 1 | 21h |
| ops-diagnostician | 5 | 2 | 2 | 1 | 0 | 10.5h |
| qa-tester | 5 | 3 | 2 | 0 | 0 | 7.5h |
| doc-writer | 4 | 1 | 2 | 1 | 0 | 6h |
| **合计** | **40** | **16** | **15** | **6** | **3** | **69h** |

## 执行顺序建议

```
第 1 批（P0 无依赖）：GB-001, GB-005, OD-001, OD-002, OD-003, OD-004
第 2 批（P0 有依赖）：GB-002, GB-006, GB-007, GB-010, VF-011
第 3 批（P0 前端）：VF-001, VF-006, VF-007, VF-008
第 4 批（P1 功能）：GB-003, GB-004, GB-008, GB-009, GB-012, GB-013
第 5 批（P1 前端）：VF-002, VF-003, VF-009, VF-010
第 6 批（测试）：QA-001, QA-002, QA-003, QA-004, QA-005
第 7 批（文档）：DW-001, DW-002, DW-003, DW-004
第 8 批（P2/P3）：GB-011, GB-014, GB-015, VF-004, VF-005, OD-005
```
