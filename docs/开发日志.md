# AI WorkBench 开发日志

## 2026-04-29 Dify 集成规划

完成 Dify 源码分析和集成设计方案，确认以下关键技术决策：
- Dify 已原生支持 MySQL（DB_TYPE=mysql），可复用现有数据库
- Dify API 作为 sidecar 运行在 :5001，Go 后端通过内部 HTTP 代理调用
- 新增 4 张表：diagnosis_cases / diagnosis_feedback / ai_settings / runbooks
- 新增 3 个后端模块：internal/dify/ / internal/metrics/normalizer/ / internal/knowledge/
- 新增 3 个前端页面：AI 设置 / 诊断交互 / 知识库管理
- 从运维角度补充 7 个优化点：指标动态发现、时间维度对比、Runbook 联动、多主机关联、告警驱动、报告对比、降级策略

### 多 Agent 协作体系 v2.0

- 5 个子 Agent 人格全部重写，融入 skills 能力：
  - go-backend：golang-patterns + api-design + database-migrations + security-review
  - vue-frontend：frontend-patterns + frontend-design + 可访问性
  - qa-tester：e2e-testing + verification-loop + security-review + 统一测试基准引用
  - ops-diagnostician：运维实战经验 + 5 种 exporter 指标体系 + 知识库设计原则
  - doc-writer：文档体系全景 + 更新检查清单
- CLAUDE.md 新增"防屎山强约束"章节：写前必做 3 条 + 绝对禁止 7 条 + 提交自检 5 条
- 每个 Agent 新增 AIOps 平台特有约束（降级/指标安全/诊断幂等/知识库一致性/审计追踪）
- 任务看板按角色分组，40 个任务卡片，总工时 69h

详细设计见 `docs/Dify集成设计方案.md`。

## 2026-04-29 Dify 集成 Batch 1-8 实施完成

### 多 Agent 协作执行

按 8 批次推进，主 Agent 编排，子 Agent 执行（go-backend/ops-diagnostician 用 opus，qa-tester 用 sonnet）：

| 批次 | 内容 | 子 Agent | 模型 |
|------|------|---------|------|
| Batch 1 | store 拆分 + DDL + Dify 代理层 + Prompt + 种子数据 | go-backend, ops-diagnostician | opus |
| Batch 2 | 案例 CRUD + 工作流执行 + 指标扫描 + 路由注册 | go-backend, vue-frontend | opus |
| Batch 3 | 知识库管理页 + 诊断工作流页 | vue-frontend | opus |
| Batch 4 | Runbook CRUD + 反馈 + 归档 + AI 设置 | go-backend（主 Agent 接管） | opus |
| Batch 5 | Runbook 页 + 指标映射页 + Dify 配置页 | 主 Agent 直接实现 | opus |
| Batch 6 | API 22 项 + UI 11 项全面烟测 | qa-tester | sonnet |
| Batch 7 | 文档全面更新 | doc-writer | opus |
| Batch 8 | AI 自动适配 + 告警驱动诊断 + 报告对比 | 主 Agent 直接实现 | opus |

### 后端架构变更

**store.go 拆分**：1039 行单文件 → 8 个独立文件（init/diagnose_records/agents/credentials/alerts/chat/topology/audit），全部 ≤ 250 行。

**新增模块**：
- `internal/dify/` — Dify API 代理层（client/workflow/knowledge/feedback）
- `internal/metrics/` — Prometheus 指标扫描器

**新增 model**：knowledge.go / metrics.go / ai_settings.go

**handler 新增 9 个文件**：knowledge.go, knowledge_sync.go, runbooks.go, feedback.go, archive.go, ai_settings.go, diagnosis_workflow.go, metrics_handler.go, metrics_adapt.go, diagnose_compare.go

### 数据库变更

新增 5 张表：
- `diagnosis_cases` — 诊断案例（含 FULLTEXT 索引）
- `diagnosis_feedback` — 诊断反馈
- `ai_settings` — AI 配置（key-value）
- `runbooks` — 运维手册
- `metrics_mappings` — 指标映射

### 新增 API 端点（30+ 个）

**知识库案例（9）**：GET/POST/PUT/DELETE /api/v1/knowledge/cases；GET /export；POST /import；GET /:id；POST /:id/sync

**Runbook（6）**：GET/POST/PUT/DELETE /api/v1/knowledge/runbooks；GET /:id；POST /:id/sync

**诊断（5）**：POST /diagnosis/start；POST /diagnosis/feedback；GET /diagnosis/feedback；GET /diagnosis/feedback/all；POST /diagnosis/archive；GET /diagnose/compare

**指标（5）**：POST /metrics/scan；POST /metrics/auto-adapt；GET /metrics/mappings；PUT /metrics/mappings/:id；POST /metrics/mappings/confirm

**AI 设置（5）**：GET/PUT /settings/ai；GET/PUT/DELETE /settings/ai/:key

### 新增前端页面（5 个）

- `KnowledgeBase.vue`（221 行）— 案例库 CRUD + 搜索筛选 + 导入导出 + Dify 同步
- `Runbooks.vue`（约 250 行）— Runbook CRUD + 详情查看
- `DiagnosisWorkflow.vue`（276 行）— 7 节点流水线 + SSE 流式 + 降级提示
- `MetricsMapping.vue`（约 240 行）— 指标扫描 + 状态筛选 + AI 适配按钮
- `DifySettings.vue`（约 130 行）— Dify 配置表单 + 敏感字段掩码

新增组件：CaseDetailDialog / CaseEditDialog / DiagnosisReport + caseHelpers.js

### 设计资产

- `assets/prompts/metrics_adapt.txt` — AI 指标适配 Prompt（5 种 exporter）
- `assets/prompts/diagnosis_system.txt` — 诊断 system prompt（结构化 JSON）
- `assets/dify_workflow.yaml` — 7 节点工作流 DSL
- `assets/seed_cases.json` — 18 条种子案例（覆盖 12 个故障场景）
- `assets/seed_runbooks.json` — 10 条 Runbook 种子数据
- `assets/tools/get_prometheus_metrics.yaml` — OpenAPI 工具定义
- `assets/tools/get_inspection_report.yaml` — OpenAPI 工具定义

### 关键能力

1. **AI 智能指标适配** — 扫描 Prometheus → AI 批量适配 → 用户确认 → 同步知识库
2. **Dify 工作流集成** — RAG 检索历史案例 + 实时指标 + LLM 分析 + 置信度分级
3. **降级策略** — Dify 不可用时返回 503 + fallback=true，前端提示降级到智能对话
4. **审计追踪** — 所有写操作记录 audit_event
5. **API Key 掩码** — 敏感字段只显示前 6 位 + "..."
6. **告警驱动诊断** — 告警 webhook 接收后自动创建诊断记录
7. **诊断报告对比** — 同主机最近 N 次诊断的 diff 摘要

### 烟测结果

API 22 项 + UI 11 项 = 33 项全部 PASS（100% 通过率）。
- MySQL 17 张表全部就位
- 18 条种子案例 + 10 条 Runbook 导入成功
- 546 条 Prometheus 指标扫描入库
- 敏感字段掩码生效
- 原有功能（Dashboard/Workbench/Diagnose/Alerts/Topology）回归零破坏

### 文件统计

- 后端新增/修改：35+ 文件
- 前端新增：8 文件（5 页面 + 3 组件 + 1 helper）
- 设计资产：8 文件
- 数据库表：5 张新表
- API 端点新增：30+ 个

### 同步更新的下载功能修复

- 后端 `renderBusinessInspectionMarkdown` — 清理 Summary 中的 JSON 乱码，包含完整 AIAnalysis
- Diagnose.vue / Topology.vue 下载函数 — 结构化数据时生成含表格的完整 Markdown
- 单机诊断也生成结构化 raw_report（buildHostInspection）
- 前端 parsedInspection 去掉 source 限制，所有有 raw_report 的记录都走结构化展示

### AIOps 拓扑分析增强

- `runAIOpsTopologyAnswer` 从简略 Summary 改为详细分层节点表格 + 风险分析表格
- 新增 `buildTopologyDetailedAnswer` 函数

---

## 2026-04-29 工作流引擎优化（Batch 1-7）

### 概述

本轮完成两大块优化：（1）原生工作流引擎替代 Dify Docker 部署；（2）知识库重构 + Embedding + 引擎增强。目标是零外部依赖，所有能力编译进单一 Go 二进制。

### 第一阶段：原生工作流引擎

用原生 Go 完整还原 Dify 的 graphon 工作流引擎，编译进二进制，零外部依赖。

**Batch 1: 核心引擎 engine/ 包（5 个文件 ~960 行）**

| 文件 | 行数 | 职责 |
|------|------|------|
| graph.go | 222 | DAG 图结构 + Kahn 拓扑排序 + 环检测 |
| variable.go | 197 | 线程安全变量池 + `{{nodeID.field}}` 嵌套路径插值 |
| streaming.go | 117 | SSE 事件发射器（6 种事件类型） |
| engine.go | 245 | 阻塞/流式执行 + 单节点重试 + 超时控制 |
| dsl.go | 177 | YAML DSL 解析 + embed.FS 内置工作流加载 |

**Batch 2: 节点类型 node/ 包（7 个文件 ~1675 行）**

| 文件 | 行数 | 节点类型 |
|------|------|---------|
| registry.go | 150 | 18 种节点注册 + LLMClient/KnowledgeSearcher/ToolExecutor 接口 |
| basic.go | 227 | start / end / condition / variable_aggregator / variable_assigner |
| llm.go | 336 | llm / parameter_extractor / question_classifier |
| data.go | 247 | knowledge_retrieval / http_request / tool |
| flow.go | 339 | loop / iteration / list_filter / template_transform |
| code.go | 162 | goja JS 沙箱 + 13 种危险模式拦截 |
| agent.go | 214 | 工具调用循环（最多 15 轮） |

新增依赖：`github.com/dop251/goja`（纯 Go JS 运行时，无 CGO）。

**Batch 3: 预置运维工作流（7 个 YAML）**

| 工作流 | 用途 |
|--------|------|
| diagnosis.yaml | 主诊断（知识检索→指标→巡检→LLM→置信度路由） |
| alert_diagnosis.yaml | 告警驱动自动诊断 |
| business_inspection.yaml | 业务巡检 |
| metrics_analysis.yaml | Prometheus 深度分析 + JS 异常检测 |
| knowledge_enrich.yaml | 知识库自动沉淀（反馈闭环） |
| runbook_execute.yaml | Runbook 安全执行（危险命令白名单） |
| capacity_forecast.yaml | 容量预测（线性回归 + LLM 解读） |

**Batch 4: Handler 集成 + API 改造**

- `bridge.go`（342 行）— LLMBridge + KnowledgeBridge + ToolBridge 桥接层，注入 LLM/知识库/工具接口
- `diagnosis_workflow.go`（100 行）— 重写，从 Dify 调用替换为原生引擎
- `workflow_handler.go`（260 行）— 8 个工作流管理 API
- `store/workflows.go`（159 行）— workflows + workflow_runs 表
- `model/workflow.go`（26 行）— 数据模型
- `main.go` — 注册 8 条新路由
- `feedback.go` — accurate 反馈自动触发 knowledge_enrich 工作流

**Batch 5: 前端适配**

- `DiagnosisWorkflow.vue`（285 行）— SSE 事件处理适配原生引擎格式
- `WorkflowManager.vue`（287 行）— 工作流管理页（列表 + DSL 查看 + 执行 + 历史）
- `router/index.js` — 新增 /workflows 路由
- `App.vue` — 侧边栏新增"工作流管理"

### 第二阶段：知识库重构 + Embedding + 引擎增强

**Batch 1: Embedding 引擎（internal/embedding/，6 个文件 ~1117 行）**

| 文件 | 行数 | 职责 |
|------|------|------|
| bm25.go | ~200 | BM25 引擎 + 中文分词（gse）+ IDF 计算 |
| vector.go | ~180 | 外部向量 API 客户端（OpenAI 兼容接口） |
| reranker.go | ~150 | LLM Reranker + API Reranker + 评分归一化 |
| hybrid.go | ~250 | 混合搜索（BM25 + 向量）+ RRF 融合排序 |
| index.go | ~180 | 索引管理（创建/更新/删除/重建） |
| types.go | ~157 | 统一类型定义 |

新增依赖：`github.com/go-ego/gse`（中文分词）。

**Batch 2: 知识库文档管理（internal/knowledge/，3 个文件 ~526 行）**

| 文件 | 行数 | 职责 |
|------|------|------|
| parser.go | ~200 | 文档解析器（MD/TXT/PDF/DOCX/HTML） |
| chunker.go | ~180 | 智能分块器（段落/标题/固定长度 + 重叠窗口） |
| indexer.go | ~146 | 文档索引管理（解析→分块→Embedding→入库） |

配套：`store/documents.go`（knowledge_documents 表）+ `handler/documents.go`（7 个 API）。

**Batch 3: Runbook 增强**

- `store/runbook_exec.go` — runbook_executions 表（执行历史 + 状态追踪）
- Runbook 模型增强：版本控制 + 模板变量（`{{var}}` 插值）+ 回滚步骤
- 新增 2 个 API：执行 Runbook + 查询执行历史

**Batch 4: 引擎并行执行 + 新工作流**

- `engine.go` 增强 — parallel_group 节点支持（并行分支 + 汇聚等待）
- 新增 5 个工作流：log_analysis / slow_query_diagnosis / network_check / security_audit / incident_timeline

**Batch 5: 工作流执行缓存**

- `workflow/cache.go`（~200 行）— Redis 缓存 + 内存 fallback + TTL 控制

**Batch 6: 前端适配**

- 知识库三标签页（案例库 / 文档管理 / 搜索测试）
- Runbook 执行面板 + 执行历史标签页
- 工作流可视化画布（DAG 节点拖拽 + 连线 + 状态着色）
- 新增 7 个 Vue 组件

**Batch 7: 配置 + 文档**

- `config.yaml` 新增 embedding / reranker / workflow.cache 配置段
- 数据库新增 2 张表（knowledge_documents + runbook_executions）
- 全部文档更新

### 数据库变更汇总

本轮新增 4 张表（工作流引擎 2 + 知识库重构 2），累计 21 张：

| 表名 | 来源 | 用途 |
|------|------|------|
| workflows | 工作流引擎 | 工作流定义（YAML DSL + 元数据） |
| workflow_runs | 工作流引擎 | 工作流执行记录（状态 + 输入输出 + 耗时） |
| knowledge_documents | 知识库重构 | 文档管理（文件元数据 + 分块 + Embedding 状态） |
| runbook_executions | 知识库重构 | Runbook 执行历史（参数 + 状态 + 输出） |

### API 端点汇总

本轮新增 17 个端点（工作流 8 + 文档 7 + Runbook 执行 2），累计 ~48 个新增端点。

### 文件统计

| 维度 | 工作流引擎 | 知识库重构 | 合计 |
|------|-----------|-----------|------|
| 新增 Go 文件 | 17 | 12 | 29 |
| 新增 YAML | 7 | 5 | 12 |
| 新增 Vue 页面/组件 | 2 | 7 | 9 |
| 新增 API 端点 | 8 | 9 | 17 |
| 新增数据库表 | 2 | 2 | 4 |
| Go 代码量 | ~4500 行 | ~2100 行 | ~6600 行 |
| 外部依赖 | goja | gse | 2 |

---

## 2026-04-27 会话变更记录

### 一、前端重构：诊断报告结构化展示

问题：诊断报告弹窗（Diagnose.vue）用 v-html 直接渲染 Markdown，没有结构化布局，不可滚动，与业务巡检弹窗（Topology.vue）结构不一致。

变更：
- 新建 `web/src/components/InspectionDashboard.vue` 公共组件，从 Topology.vue 提取结构化巡检展示（hero 区域 + 6 个 el-tabs）
- 改造 `web/src/views/Diagnose.vue`：drawer 内容区接入 InspectionDashboard，业务巡检记录从 raw_report 解析 JSON 走结构化展示，其他类型降级 Markdown 渲染，加 max-height + overflow-y 滚动
- 改造 `web/src/views/Topology.vue`：内联巡检展示替换为组件引用，删除搬迁的代码和样式（减少 104 行）
- 修复 Markdown 渲染的表格无边框样式问题（.ai-advice 加 table/th/td deep 样式）
- 修复 hero 摘要区域显示 JSON 乱码问题（cleanSummary computed 过滤 JSON）
- el-tabs 加 lazy 属性避免隐藏 tab 中 el-table 高度为 0

涉及文件：
- `web/src/components/InspectionDashboard.vue`（新建，136 行）
- `web/src/views/Diagnose.vue`（265 行）
- `web/src/views/Topology.vue`（816 行，从 920 行减少）

### 二、MySQL 安全加固

变更：
- MySQL root 用户设置密码 `Iqtc@2026`
- 认证插件从 `auth_socket` 改为 `mysql_native_password`
- `api/config.yaml` DSN 从 Unix socket 改为 TCP 格式：`root:Iqtc@2026@tcp(127.0.0.1:3306)/ai_workbench`

### 三、config.yaml 新增配置项

1. `server.host`：API 监听地址，默认 `0.0.0.0`，可改为 `127.0.0.1` 仅本机
2. `server.server_ip`：平台主机 IP，默认 `172.20.32.65`，影响 GetPlatformIP 接口和拓扑种子节点

涉及文件：
- `api/config.yaml`
- `api/main.go`（读取 server.host 拼接监听地址）
- `api/internal/handler/platform.go`（PlatformIP() 优先读 server.server_ip）
- `api/internal/handler/persist.go`（拓扑种子节点用 PlatformIP()）

### 四、测试基准整合

将 docs/testing/ 下 17 份测试文档整合为一份统一测试基准：
- `docs/testing/AI_WorkBench_统一测试基准.md`（1153 行，26 章 + 4 附录，约 540 用例）
- 新增 11 个缺失测试领域：灾备降级、性能压力、网络异常、多用户并发、数据边界、通知渠道、日志审计、用户体验、数据备份、升级回滚、定时任务
- `docs/testing/README.md` 更新指向统一基准

### 五、项目文档更新

- `docs/运维手册.md`：MySQL 连接方式/密码/备份命令/config 示例/安全注意事项全部更新
- `README.md`：重写，清理乱码，更新运行环境/配置/测试规范

---

## 2026-04-29 v2 端到端测试

### 一、测试范围

完成 AI WorkBench v2 的端到端穷举烟测，覆盖 10 大模块：

1. 测试环境与数据基线（10 用例）
2. Embedding/Reranker 配置与连接（15 用例）
3. 知识库 CRUD + 语义搜索 + 文档上传（30 用例）
4. 18 个工作流全量调用（36 用例 = 18 × 2 模式）
5. 告警 → 自动诊断 → 归档闭环（30 用例）
6. 指标扫描 + AI 适配 + 知识库同步（15 用例）
7. Runbook CRUD + 执行 + 历史 + 模板变量（20 用例）
8. 前端 8 个主页面 UI 烟测（25 用例）
9. 缓存命中率 + 并行执行验证（10 用例）
10. 降级与异常处理（15 用例）

合计 **195 条手工基准用例 + 35 条自动化用例**。

### 二、关键结果

- **自动化通过率：100%（35/35 PASS）**
- **数据基线**：30 案例 / 18 Runbook / 119 文档 / 18 工作流 / 546 指标映射 / 15 诊断记录
- **工作流**：12 个核心 + 6 个扩展全部 PASS
- **告警闭环**：CPU/OOM/磁盘/慢 SQL/网络丢包 5 场景 → 5 自动诊断
- **语义搜索**：8 场景全部命中
- **缓存提速**：首次 28,675ms → 命中 24ms（≈ 1,200×）

### 三、Embedding/Reranker 配置

- Provider：阿里云 DashScope `text-embedding-v3`
- 维度：1024
- Engine：hybrid（自动 fallback 到 bm25 / fulltext）
- Reranker：启用，provider=api，top_k=5

### 四、性能数据

| 项目 | 数值 |
|------|------|
| 工作流首次执行 | 平均 22 s |
| 工作流缓存命中 | < 50 ms（典型 24 ms） |
| 语义搜索 | 平均 3 ms |
| 健康检查 | 12 ms |

### 五、已知问题（中等及以下）

1. 语义搜索 score 显示为 0.000（fulltext 引擎未归一化输出）
2. 当前 engine=fulltext 而非 hybrid（placeholder key 下 fallback）
3. Reranker 网络重试日志冗余

### 六、文档产出

- `docs/testing/v2_穷举烟测基准.md`：10 章节 / 195 条用例 / 4 个附录
- `docs/testing/v2_测试报告.md`：35 项自动化结果 + 性能数据 + 关键发现 + 已知问题 + 下一步建议

### 七、下一步

1. 注入真实 Embedding API Key 重跑混合搜索基准
2. 工作流可视化编辑器（基于现有 18 个 YAML）
3. 告警二次升级（增加抑制规则）
4. 指标映射对账（546 vs Prometheus 实际全集）
5. Runbook 灰度执行流程
6. 前端可达性（axe-core）扫描
7. AI WorkBench 自身指标 self-monitoring
